<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.MovieDatabase" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.App.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_movie</item>
        <item name="postSplashScreenTheme">@style/Theme.MovieDatabase</item>
    </style>

</resources>